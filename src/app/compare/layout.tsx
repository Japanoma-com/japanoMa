export const metadata = {
  title: 'Compare Areas',
  description: 'Side-by-side comparison of snow country areas.',
  alternates: { canonical: '/compare' },
};

export default function CompareLayout({ children }: { children: React.ReactNode }) {
  return children;
}
