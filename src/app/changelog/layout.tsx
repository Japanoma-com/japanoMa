export const metadata = {
  title: 'Changelog',
  description: 'Development updates, new features, and improvements to Japanoma.',
  alternates: { canonical: '/changelog' },
};

export default function ChangelogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
